---
import ElevatorTimeline from './ElevatorTimeline.jsx';
const { items } = Astro.props;
---

<!-- React elevator timeline, loads on client for animation -->
<ElevatorTimeline items={items} client:load />

